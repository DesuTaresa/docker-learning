services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: flaskdb
      POSTGRES_USER: flaskuser
      POSTGRES_PASSWORD: flaskpassword
    ports:
      - "5433:5432"

  api:
    build: .
    ports:
      - "5003:5000"